Paths:
df=pd.read_csv('data/telecom_users_cleaned.csv')#, index_col=0)


Data processing:
X_train_, X_test_, y_train, y_test = train_test_split(df.drop(columns='Churn') # change in target



Comment out:
- Installations lines
- Platform-specific fixes/workaround

df[target_name].value_counts()
df_cont['tenure']
df.isnull().sum()
df['Churn']
df.isnull().sum()
df.dropna(inplace=True)
X_scaled.savez()